{
  "name": "cf-workers-proxy",
  "description": "Enable Cloudflare Workers runtime for local development. Compatible with DrizzleORM",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "watch": "node watch.js",
    "build": "rm -rf dist && node build.js && npx tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230814.0",
    "esbuild": "^0.19.2",
    "mimetext": "^3.0.16",
    "typescript": "^5.1.6",
    "wrangler": "^3.5.1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/chientrm/cf-workers-proxy.git"
  },
  "author": "chientrm",
  "files": [
    "dist"
  ],
  "keywords": [
    "cloudflare",
    "workers",
    "proxy",
    "local",
    "development"
  ]
}
